# Architecture

1. Parse Markdown/QMD and YAML frontmatter.
2. Resolve wiki links and backlinks.
3. Filter by privacy mode.
4. Render semantic HTML.
5. Emit search-index.json, manifest.json, and chunks.jsonl.
