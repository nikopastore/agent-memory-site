# HTML vs Markdown for Agent Memory

Markdown is best for authoring. Semantic HTML is best as a compiled interface and retrieval substrate. HTML carries section boundaries, table semantics, metadata, time elements, figures, and accessibility structure that chunkers and browser agents can use. The recommended pattern is Markdown source → semantic HTML → JSONL retrieval chunks.
