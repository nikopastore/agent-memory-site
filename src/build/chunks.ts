import type { Chunk, Note } from '../types.js';
export function chunkNote(note:Note):Chunk[]{const sections=note.body.split(/\n(?=#{2,3}\s+)/g).map(s=>s.trim()).filter(Boolean);return sections.map((s,i)=>{const heading=(s.match(/^#{1,3}\s+(.+)$/m)?.[1]||note.title).trim();const text=s.replace(/^#{1,6}\s+/gm,'').replace(/\s+/g,' ').trim();return{chunk_id:`${note.id}#${i+1}-${heading.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'')}`,doc_id:note.id,title:note.title,heading_path:[note.title,heading],type:String(note.meta.type||note.category),text,html:`<section><h2>${escapeHtml(heading)}</h2><p>${escapeHtml(text)}</p></section>`,source_path:note.relPath,canonical_url:`notes/${note.slug}.html`,updated:String(note.meta.updated||note.meta.date||''),tags:Array.isArray(note.meta.tags)?note.meta.tags.map(String):[],tokens:Math.ceil(text.length/4)}})}
function escapeHtml(s:string){return s.replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]!))}
