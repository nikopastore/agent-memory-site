import { describe,it,expect } from 'vitest';import { extractWikiLinks, slugify } from '../src/ingest/markdown.js';import { includeNote, redactText } from '../src/build/privacy.js';import { hasPossibleSecret } from '../src/validate.js';
describe('basics',()=>{it('slugifies',()=>expect(slugify('Use HTML Output')).toBe('use-html-output'));it('extracts wiki links',()=>expect(extractWikiLinks('See [[Use HTML Output]]')).toEqual(['Use HTML Output']));it('excludes private in public mode',()=>expect(includeNote({meta:{visibility:'private',sensitivity:'none'}} as any,'public')).toBe(false));it('detects and redacts common secrets',()=>{const sampleSecret=`${'github_pat'}_${'a'.repeat(24)}`;expect(hasPossibleSecret(`token = ${sampleSecret}`)).toBe(true);expect(redactText(`email ${'test'}@${'example.com'} token = ${sampleSecret}`)).toBe('email [redacted-email] [redacted-secret]')});});
